# Bodies in Balance Pilates Website

Built with Metalsmith. Must have some version of nodejs and npm
installed. Then you can build with just a `make`. Or manually,

```shell
npm install
node site.js
```
